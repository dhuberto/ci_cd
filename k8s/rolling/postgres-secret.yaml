# =============================================================================
# postgres-secret.yaml — credenciais do Postgres (namespace rolling)
# =============================================================================
# Em produção real, use Sealed Secrets ou External Secrets Operator.
# Aqui é um Secret simples para o lab.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: rolling
type: Opaque
stringData:
  POSTGRES_USER:     appuser
  POSTGRES_PASSWORD: mudar123
  POSTGRES_DB:       appdb
