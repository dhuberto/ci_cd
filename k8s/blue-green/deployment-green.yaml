# =============================================================================
# deployment-green.yaml — slot GREEN
# =============================================================================
# APP_COLOR=green faz a interface ficar verde quando o slot está ativo.
# labels slot=green casam com o Service app-go-green e com o selector
# do Service app-go-active (durante o switch).
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-go-green
  namespace: blue-green
  labels:
    app: app-go
    slot: green
spec:
  replicas: 2
  selector:
    matchLabels:
      app: app-go
      slot: green
  template:
    metadata:
      labels:
        app: app-go
        slot: green
    spec:
      serviceAccountName: app-go-sa
      containers:
        - name: app
          image: IMAGE_PLACEHOLDER
          imagePullPolicy: Always
          ports:
            - containerPort: 5000
          env:
            - name: APP_NAME
              value: "APP-GO Green"
            - name: APP_COLOR
              value: "green"
            - name: APP_PORT
              value: "5000"
            - name: DB_HOST
              value: "postgres"
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: "appuser"
            - name: DB_PASSWORD
              value: "mudar123"
            - name: DB_NAME
              value: "appdb"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 10
