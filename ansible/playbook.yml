---
# Configura a EC2 para receber deploys.
# Ordem: Docker (com fallback) → kind → kubectl → cluster kind →
# ingress-nginx → namespaces rolling e blue-green.
#
# Uso de `command` com caminho absoluto em vez de `shell`: o SSH não-interativo
# não carrega /etc/profile.d/, então /usr/local/bin pode não estar no PATH.
#
# kubectl com versão fixa: evita HTTP discovery (uri) que pode falhar no meio.
- name: Configurar EC2 para o lab de CD
  hosts: ec2
  gather_facts: true
  become: false

  vars:
    kind_version: "v0.24.0"
    kind_cluster_name: "devops-labs"
    kubectl_version: "v1.31.0"

  tasks:
    # Verifica se docker existe; se não, instala com --allowerasing.
    - name: Verificar se docker existe
      command: docker --version
      register: docker_check
      failed_when: false
      changed_when: false

    - name: Instalar docker (fallback se user_data falhou)
      become: true
      dnf:
        name: docker
        state: present
        allowerasing: true
      when: docker_check.rc != 0

    # Garante que o serviço está habilitado e rodando.
    - name: Garantir docker habilitado e rodando
      become: true
      systemd:
        name: docker
        enabled: true
        state: started

    # Garante que o ec2-user pode usar docker sem sudo.
    - name: Garantir que o ec2-user está no grupo docker
      become: true
      user:
        name: ec2-user
        groups: docker
        append: true

    # ---- kind ----
    - name: Instalar kind em /usr/local/bin
      become: true
      get_url:
        url: "https://kind.sigs.k8s.io/dl/{{ kind_version }}/kind-linux-amd64"
        dest: /usr/local/bin/kind
        mode: "0755"
        force: true

    - name: Verificar kind instalado
      stat:
        path: /usr/local/bin/kind
      register: kind_bin

    - name: Falhar se kind não existe
      fail:
        msg: "kind não foi instalado em /usr/local/bin/kind"
      when: not kind_bin.stat.exists

    # ---- kubectl ----
    - name: Instalar kubectl em /usr/local/bin
      become: true
      get_url:
        url: "https://dl.k8s.io/release/{{ kubectl_version }}/bin/linux/amd64/kubectl"
        dest: /usr/local/bin/kubectl
        mode: "0755"
        force: true

    - name: Verificar kubectl instalado
      stat:
        path: /usr/local/bin/kubectl
      register: kubectl_bin

    - name: Falhar se kubectl não existe
      fail:
        msg: "kubectl não foi instalado em /usr/local/bin/kubectl"
      when: not kubectl_bin.stat.exists

    - name: Confirmar que kubectl executa
      command: /usr/local/bin/kubectl version --client
      register: kubectl_check
      changed_when: false

    - name: Mostrar versão do kubectl
      debug:
        msg: "{{ kubectl_check.stdout_lines }}"

    # ---- kind-config.yaml ----
    # Expõe as portas 80/443 do nó para que o ingress-nginx responda
    # diretamente no IP da EC2.
    - name: Criar kind-config.yaml
      copy:
        dest: /home/ec2-user/kind-config.yaml
        owner: ec2-user
        group: ec2-user
        mode: "0644"
        content: |
          kind: Cluster
          apiVersion: kind.x-k8s.io/v1alpha4
          networking:
            apiServerAddress: "0.0.0.0"
            apiServerPort: 6443
          nodes:
            - role: control-plane
              kubeadmConfigPatches:
                - |
                  kind: InitConfiguration
                  nodeRegistration:
                    kubeletExtraArgs:
                      node-labels: "ingress-ready=true"
              extraPortMappings:
                - { containerPort: 80,  hostPort: 80,  protocol: TCP }
                - { containerPort: 443, hostPort: 443, protocol: TCP }

    # ---- cluster kind (idempotente) ----
    - name: Verificar se o cluster kind já existe
      command: /usr/local/bin/kind get clusters
      register: kind_clusters
      failed_when: false
      changed_when: false

    - name: Criar cluster kind
      command: >
        /usr/local/bin/kind create cluster
        --name {{ kind_cluster_name }}
        --config /home/ec2-user/kind-config.yaml
      when: kind_cluster_name not in kind_clusters.stdout

    - name: Aguardar o nó ficar Ready
      command: /usr/local/bin/kubectl wait --for=condition=Ready node --all --timeout=180s
      changed_when: false

    # ---- ingress-nginx ----
    - name: Aplicar manifest do ingress-nginx (kind)
      command: >
        /usr/local/bin/kubectl apply -f
        https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/kind/deploy.yaml

    - name: Aguardar ingress-nginx
      command: >
        /usr/local/bin/kubectl wait --namespace ingress-nginx
        --for=condition=ready pod
        --selector=app.kubernetes.io/component=controller
        --timeout=240s
      changed_when: false

    # ---- namespaces ----
    - name: Criar namespaces do lab
      command: /usr/local/bin/kubectl create ns {{ item }}
      register: ns_create
      failed_when: >
        ns_create.rc != 0 and
        'already exists' not in (ns_create.stderr | default(''))
      changed_when: ns_create.rc == 0
      loop:
        - rolling
        - blue-green

    # Log final — aparece no Actions.
    - name: Mostrar nodes
      command: /usr/local/bin/kubectl get nodes -o wide
      register: nodes_out
      changed_when: false

    - name: Output nodes
      debug:
        msg: "{{ nodes_out.stdout_lines }}"
